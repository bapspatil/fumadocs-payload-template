# NextJS Public App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Payload CMS Configuration
PAYLOAD_SECRET=your-secret-key-here

# ============================================================================
# FOR VERCEL DEPLOYMENT (Primary - uses MongoDB + S3)
# ============================================================================

# MongoDB Database
PAYLOAD_DATABASE_URI=mongodb://localhost:27017/payload-cms
PAYLOAD_DATABASE_CA_FILE=
PAYLOAD_DATABASE_CERT_FILE=
PAYLOAD_DATABASE_KEY_FILE=

# S3 Storage (AWS S3, DigitalOcean Spaces, or compatible)
S3_BUCKET=your-bucket-name
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key
S3_REGION=auto
S3_ENDPOINT=https://your-s3-endpoint.com
S3_FORCE_PATH_STYLE=false

# ============================================================================
# FOR CLOUDFLARE WORKERS DEPLOYMENT (Secondary - uses D1 + R2)
# ============================================================================

# Enable Cloudflare Workers mode
# DEPLOYMENT_TARGET=cloudflare

# D1 Database (SQLite for Cloudflare Workers)
# For production, the D1 binding will be provided automatically
# PAYLOAD_CLOUDFLARE_D1_BINDING=payload-db

# R2 Storage (Cloudflare R2)
# The bucket binding will be provided by wrangler.jsonc
# PAYLOAD_CLOUDFLARE_R2_BUCKET=payload-media

# ============================================================================
# EMAIL (Optional - for both deployments)
# ============================================================================

# Email (Nodemailer)
SMTP_HOST=smtp.example.com
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
